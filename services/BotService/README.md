# Bot Service - Telegram Notification Bot

Telegram бот для управления уведомлениями от различных сервисов (GitHub, StackOverflow и др.).

## Возможности

- 🔐 Авторизация в сервисах (GitHub, StackOverflow)
- 📬 Подписка на уведомления (Issues, Pull Requests, Commits и др.)
- ⏰ Настройка периода получения уведомлений
- 🏷 Управление тегами
- 📜 История уведомлений
- 📡 **Прослушивание топика Kafka "Notifications"** для получения уведомлений в реальном времени

## Архитектура

Бот работает в двух режимах одновременно:
1. **Telegram Bot** - обрабатывает команды и взаимодействие с пользователями
2. **Kafka Consumer** - слушает топик "Notifications" и отправляет уведомления пользователям

## Установка

```bash
# Установить зависимости
pip install -e .

# Или через uv
uv pip install -e .
```

## Настройка

Создайте файл `.env` на основе `.env.example`:

```env
BOT_TOKEN=your_telegram_bot_token
DB_API_URL=http://localhost:8080/api

# Kafka settings
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_GROUP_ID=bot-service-group
```

### Переменные окружения

- `BOT_TOKEN` - токен Telegram бота (получить у @BotFather)
- `DB_API_URL` - URL API базы данных (по умолчанию Spring Boot запускается на порту 8080)
- `KAFKA_BOOTSTRAP_SERVERS` - адрес Kafka broker(ов), разделенные запятой
- `KAFKA_GROUP_ID` - ID группы consumer'а Kafka

## Запуск

```bash
python main.py
```

## Kafka интеграция

### Топик: `Notifications`

Бот автоматически подключается к Kafka и слушает топик `Notifications`. 

### Формат сообщений

Сообщения в топике должны быть в JSON формате:

```json
{
  "telegram_id": 123456789,
  "title": "Новый issue",
  "message": "Создан новый issue в вашем репозитории",
  "service": "github",
  "type": "issue",
  "url": "https://github.com/user/repo/issues/123"
}
```

**Обязательные поля:**
- `telegram_id` - ID пользователя в Telegram

**Опциональные поля:**
- `title` - заголовок уведомления
- `message` - текст уведомления
- `service` - название сервиса (github, stackoverflow и т.д.)
- `type` - тип уведомления (issue, pull_request, commit и т.д.)
- `url` - ссылка на ресурс

### Пример отправки уведомления в Kafka

```python
import json
from kafka import KafkaProducer

producer = KafkaProducer(
    bootstrap_servers=['localhost:9092'],
    value_serializer=lambda v: json.dumps(v).encode('utf-8')
)

notification = {
    "telegram_id": 123456789,
    "title": "Новый Pull Request",
    "message": "PR #42: Добавлена новая фича",
    "service": "github",
    "type": "pull_request",
    "url": "https://github.com/user/repo/pull/42"
}

producer.send('Notifications', notification)
producer.flush()
```

## Структура проекта

```
BotService/
├── main.py            # Основной файл с ботом и Kafka consumer
├── db_client.py       # HTTP клиент для API базы данных
├── kafka_consumer.py  # Kafka consumer для топика Notifications
├── pyproject.toml     # Зависимости проекта
├── .env               # Переменные окружения (не в git)
└── .env.example       # Пример переменных окружения
```

## Логирование

Бот логирует все важные события:
- Запуск/остановка Kafka consumer
- Получение сообщений из Kafka
- Отправку уведомлений пользователям
- Ошибки обработки

## Требования

- Python >= 3.14
- Kafka broker (для production)
- API базы данных

## Зависимости

- `aiogram` - Telegram Bot API
- `aiokafka` - асинхронный Kafka клиент
- `httpx` - HTTP клиент для API запросов
- `python-dotenv` - загрузка переменных окружения
