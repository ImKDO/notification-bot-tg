# Notification Bot Telegram

Telegram-бот для агрегации и управления уведомлениями из различных сервисов (GitHub, StackOverflow и др.) с использованием микросервисной архитектуры и машинного обучения для генерации умных сводок (дайджестов).

## Структура проекта

Проект состоит из нескольких микросервисов, каждый из которых выполняет свою задачу:

- **BotService** (Python / aiogram) — Telegram-бот. Отвечает за взаимодействие с пользователем (авторизация, настройка подписок) и отправку уведомлений в реальном времени, читая их из Kafka.
- **DBService** (Kotlin / Spring Boot) — сервис для работы с базой данных (PostgreSQL). Управляет пользователями, токенами авторизации и подписками. Предоставляет REST API для других сервисов.
- **CoreService** (Kotlin / Spring Boot) — ядро бизнес-логики. Оркестрирует процессы, обрабатывает события от интеграционных сервисов и формирует итоговые уведомления для отправки в Kafka.
- **GithubService** (Kotlin / Spring Boot) — интеграция с GitHub. Отслеживает события (Issues, Pull Requests, Commits) и отправляет их в Kafka.
- **StackoverflowService** (Kotlin / Spring Boot) — интеграция со StackOverflow. Отслеживает новые ответы и комментарии, отправляя события в Kafka.
- **MLService** (Python / MLX) — сервис машинного обучения. Использует дообученную (LoRA) модель `google/gemma-3-4b-it` для генерации кратких сводок (саммари) по накопившимся уведомлениям.
- **AirflowService** (Python / Apache Airflow) — планировщик задач. Выполняет регулярные фоновые задачи:
  - Архивация истории уведомлений из Redis в формат Parquet.
  - Генерация ежедневных дайджестов (отправка данных в MLService и сохранение результатов).
  - Очистка старых архивов.

## Взаимодействие микросервисов

1. **Настройка подписок:** Пользователь взаимодействует с **BotService** через Telegram. BotService обращается к **DBService** (через REST API) для сохранения пользователя, токенов и настроек подписок.
2. **Сбор данных:** Интеграционные сервисы (**GithubService**, **StackoverflowService**) периодически опрашивают внешние API или получают вебхуки, после чего отправляют сырые события в **Kafka**.
3. **Обработка:** **CoreService** (или сами интеграционные сервисы) обрабатывает эти события, проверяет подписки пользователей через DBService и формирует готовые сообщения для отправки в топик `Notifications` в Kafka.
4. **Доставка уведомлений:** **BotService** выступает в роли Kafka Consumer, слушает топик `Notifications` и моментально отправляет сообщения пользователям в Telegram. Также история уведомлений сохраняется в **Redis**.
5. **Суммаризация (Дайджесты):** По расписанию **AirflowService** собирает накопившиеся уведомления из Redis, архивирует их в Parquet и отправляет пачки текста в **MLService**. MLService генерирует краткую сводку с помощью нейросети и возвращает её. Airflow сохраняет готовый дайджест в Redis, откуда BotService может его забрать по запросу пользователя или отправить по расписанию.

## Как запустить проект

### Требования
- Docker и Docker Compose
- Python 3.11+ (для локального запуска Python-сервисов)
- Java 21 / Kotlin (для локального запуска Spring Boot сервисов)
- `uv` (опционально, для управления Python-зависимостями)

### 1. Запуск инфраструктуры

В корне проекта находится `docker-compose.yml`, который поднимает базовую инфраструктуру:
- PostgreSQL (база данных)
- Redis (кэш и хранилище истории)
- Kafka (брокер сообщений)
- Apache Airflow (планировщик)

```bash
docker compose up -d
```

*При первом запуске PostgreSQL скрипты из папки `services/DBService/init-db/` автоматически создадут необходимые схемы и таблицы.*

### 2. Запуск микросервисов

Каждый микросервис можно запустить локально для разработки.

#### Spring Boot сервисы (DBService, CoreService, GithubService, StackoverflowService)
Для каждого из Kotlin-сервисов перейдите в соответствующую директорию и выполните запуск через Gradle:
```bash
cd services/DBService
./gradlew bootRun
```
*(Повторите для CoreService, GithubService, StackoverflowService в отдельных окнах терминала)*

#### BotService
Создайте файл `.env` в папке `services/BotService` (укажите ваш `BOT_TOKEN` от @BotFather).
```bash
cd services/BotService
pip install -e .
python main.py
```

#### MLService
Для работы MLService потребуется токен HuggingFace (для загрузки модели Gemma).
```bash
cd services/MLService
echo "HF_TOKEN=ваш_токен" > .env
uv sync
uv run uvicorn server:app --host 0.0.0.0 --port 8042
```

## 🛠 Технологический стек
- **Backend:** Kotlin, Spring Boot, Python, aiogram
- **Data & ML:** Apache Airflow, PyArrow, Parquet, MLX, HuggingFace (Gemma-3)
- **Infrastructure:** Docker, PostgreSQL, Redis, Apache Kafka
